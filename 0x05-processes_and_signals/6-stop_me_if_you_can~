#!/usr/bin/env bash
#Displays lines containing the bash word, thus allowing user to easily get the PID of user's Bash process
# shellcheck disable=SC2009

pkill -f "4-to_infinity_and_beyond" 


#pgrep -l bash

#ps -a -f -aux | grep bash  
